[Unit]
# The SD-card Pi observes the Hetzner experiment. No local fork, gossip, or
# mining process may start unless an operator deliberately creates this marker
# and reloads systemd after a capacity and cooling review.
ConditionPathExists=/etc/pohw/enable-pi-local-pohw-runtime
