[Unit]
# A durable one-way marker is written before any mainnet environment or process
# is created. This condition prevents a reboot from reviving the fork afterward.
ConditionPathExists=!/var/lib/pohw-p2pool/mainnet-handoff/mainnet-activated.json
