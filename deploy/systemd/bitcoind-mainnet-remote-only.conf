[Unit]
# The Pi consumes the Hetzner Bitcoin service. Local Core may start only after
# an operator deliberately creates this marker and reloads systemd.
ConditionPathExists=/etc/pohw/enable-local-bitcoin
