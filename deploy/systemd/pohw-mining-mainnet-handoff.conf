[Service]
# Loaded after /etc/pohw/p2pool.env. The controller creates this file only
# after the participant threshold and Bitcoin mainnet preflight both pass.
EnvironmentFile=-/var/lib/pohw-p2pool/mainnet-handoff/mining-mode.env
